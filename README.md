# X-Ray Image Classifier with ResNet-18 (PyTorch)

Fine-tune a pretrained ResNet-18 model on a chest X-ray (or similar) dataset.

Expected directory structure:

xray_data/
    train/
        class0/
        class1/
    val/
        class0/
        class1/
